import { Action, Reducer } from 'redux';

import { ToolboxState } from '../toolbox.model';

export const rotatetoolReducer: Reducer<ToolboxState> = (state: ToolboxState, action: Action) => {
	return state;
};
